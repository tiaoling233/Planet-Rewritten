<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Planet.Main.Views.MysteryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <!-- ??? 页：今日人品等功能入口 -->
    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="40">

            <TextBlock Text="???"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#2B2F36"
                       HorizontalAlignment="Center" />

            <TextBlock Text="这里藏着一些奇妙的小功能"
                       FontSize="13"
                       Foreground="#9AA0A8"
                       Margin="0,8,0,28"
                       HorizontalAlignment="Center" />

            <Button x:Name="LuckyButton"
                    Content="🎲 今日人品"
                    
                    Width="150"
                    Height="40"
                    Click="OnLuckyButtonClick" />

            <!-- 千万别点：红色边框 + 红色文本警示按钮 -->
            <Button x:Name="DoNotClickButton"
                    Content="⚠️ 千万别点"
                    
                    Width="150"
                    Height="40"
                    Margin="0,14,0,0"
                    Background="#C0392B"
                    Foreground="#FFE8E8"
                    BorderBrush="#96281B"
                    BorderThickness="1"
                    Click="OnDoNotClickButtonClick" />
        </StackPanel>
    </Grid>
</UserControl>